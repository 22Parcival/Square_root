[package]
name = "square_root"
version = "0.1.0"
edition = "2021"
description = "un nouveau format de fichier qui remplace les pixels par Root la mascotte du discord"

[dependencies]
image = "0.24"
clap = { version = "4.4", features = ["derive"] }